# Node CLI Release Path

`node-cli/` is currently a first-pass recovery package used for source and npm-bin verification. It is not yet connected to a standalone GitHub review repository or npm publishing credentials.

Before production release, attach a GitHub remote for this package, enable the CI workflow in `.github/workflows/ci.yml`, and choose a publishing owner for npm or GitHub Releases.
